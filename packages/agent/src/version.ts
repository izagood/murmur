/**
 * 러너의 빌드 버전. **환경변수로만 받는다.**
 *
 * 왜 번들러·빌드 스텝을 두지 않는가: 이 저장소의 러너는 `tsx src/main.ts` 로 소스에서
 * 바로 돈다. 버전을 굽기 위해 번들 산출물(`dist/`)을 만들고 `start` 를 그쪽으로 바꾸면,
 * 값 하나를 넣으려고 **러너가 뜨는 방식 자체**를 바꾸는 것이 된다. 환경변수는 같은 값을
 * 배포 시점에 넣으면서 기동 경로를 건드리지 않는다.
 *
 * 값이 없으면 `unknown` 이다. 거짓 버전을 보내지 않는다 —
 * `docs/design.md` 4절 "없는 것을 있다고 표시하지 않는다".
 *
 * 배포에서 설정한다(예: 커밋 sha). 설정하지 않으면 서버는 "버전을 모른다" 로 본다.
 */
export const VERSION = process.env.AGENT_VERSION ?? 'unknown';
