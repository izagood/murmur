/**
 * 플랫폼 판정(#270).
 *
 * `@tauri-apps/plugin-os` 의 `platform()` 이 더 정확하지만 이 앱은 그 플러그인을 의존하지
 * 않는다 — 창 장식 여백 하나를 위해 네이티브 플러그인을 늘릴 이유가 없다. 그래서 웹뷰가 항상
 * 주는 값만 본다.
 *
 * **실패하면 false 다.** 브라우저 개발 모드(`pnpm dev` 를 크롬에서 여는 경우)나 `navigator`
 * 가 없는 테스트 환경에서 던지면 화면이 통째로 안 뜬다 — 여백 하나를 잘못 맞히는 것보다 나쁘다.
 */
export function isMacOS(): boolean {
  try {
    if (typeof navigator === 'undefined') return false;
    // `platform` 은 폐기 예정이지만 아직 모든 웹뷰가 준다. 없는 날에도 userAgent 로 답이 난다.
    const platform = navigator.platform ?? '';
    const userAgent = navigator.userAgent ?? '';
    if (/Mac|iPhone|iPad|iPod/.test(platform)) return true;
    // Windows 의 UA 에는 `Mac` 이 없지만, 일부 브라우저가 호환용으로 다른 OS 이름을 함께
    // 흘리는 경우가 있어 명시적으로 배제한다.
    return /Mac/.test(userAgent) && !/Windows/.test(userAgent);
  } catch {
    return false;
  }
}

/**
 * macOS 신호등(닫기·최소화·최대화) 3 개와 그 좌우 여백이 차지하는 폭. `titleBarStyle:
 * "Overlay"` 는 신호등을 **콘텐츠 위에** 띄우므로, 창 좌상단에 있는 바가 이만큼을 비워 두지
 * 않으면 신호등이 그 바의 내용을 덮는다.
 *
 * 전체화면에서는 신호등이 사라지지만 이 여백은 그대로 남는다 — v1 에서 받아들인다. 전체화면
 * 상태를 추적하려면 창 이벤트 구독이 필요하고, 그것은 이 작업의 범위가 아니다.
 */
export const MAC_TRAFFIC_LIGHT_PL = 'pl-[78px]';

/**
 * OS 타이틀바가 있던 띠의 높이. 로그인 화면처럼 좌상단에 바가 없는 화면이 창 손잡이 띠를
 * 둘 때 쓴다 — `MAC_TRAFFIC_LIGHT_PL` 이 가로 여백이라면 이쪽은 세로다.
 *
 * **28px 은 눈대중이 아니라 측정값이다.** 신호등은 지름 14px 이고 세로 중심이 창 위에서
 * 약 12px 에 있다(2x 스크린샷에서 지름 28px, 중심 y=23.5px 를 읽었다). 위아래 여백을 같이
 * 두면 띠는 28px 이 된다. 초판의 38px 은 여기서 10px 이 더 컸고, 그만큼 **원래 타이틀바보다
 * 아래까지 끌리는 영역**이 되어 로그인 폼 위 빈 공간이 창 이동에 먹혔다.
 */
export const MAC_TITLEBAR_H = 'h-[28px]';

/**
 * 창 최상단 바 한 줄의 높이(#359).
 *
 * 사이드바 브랜드 바와 `Workspace` 헤더는 **가로로 나란히 붙어 한 줄처럼 보인다.** 그런데
 * 각자 세로 여백(`py-3`·`py-1`)으로 높이를 따로 만들고 있어서 아래 경계가 어긋났다 —
 * 실측으로 브랜드 바 48px, 헤더 32px 이었다.
 *
 * 여백이 아니라 **높이를 못박아** 둘이 같은 값을 쓴다. 안쪽은 `items-center` 가 세로
 * 가운데를 맞추므로, 한쪽 내용이 바뀌어도 경계는 계속 맞는다. 여백으로 맞추면 폰트나
 * 아이콘 크기가 바뀔 때마다 두 숫자를 같이 고쳐야 하고, 한쪽만 고치는 순간 다시 어긋난다.
 *
 * **36px 은 두 제약 사이에서 고른 값이다.** 위로는 옛 브랜드 바(48px)처럼 두꺼우면 타이틀바로
 * 보이지 않고 화면 위쪽을 낭비한다. 아래로는 신호등 높이에 딱 맞춘 28px(`MAC_TITLEBAR_H`)이면
 * 로고·`murmur`·`←` 가 위아래로 눌려 답답하다. 36px 은 신호등(지름 14px, 중심 y≈12px)을
 * 품으면서 그 여유를 준다.
 *
 * 손잡이 띠(`MAC_TITLEBAR_H`)와는 **일부러 다른 값이다.** 띠는 좌상단에 바가 없는 화면에서
 * "원래 타이틀바가 있던 자리"만 끌리게 하는 것이라 그 높이를 넘으면 안 되지만, 이 바는
 * 실제로 내용을 담는다.
 */
export const TOP_BAR_H = 'h-9';
