# internal working docs — never committed (any directory named superpowers)
superpowers/

node_modules/
dist/
target/
.env
.env.*
*.log
.DS_Store

# 로컬 개발용 compose 오버라이드
docker-compose.override.yml

# 로컬 실행 시 첨부가 쌓이는 곳(컨테이너는 볼륨을 쓴다).
.attachments/
